name: Costly CI + Deploy

on:
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  test:
    name: Costly CI Check
    runs-on: ubuntu-latest
    environment: costly-ci

    steps:
      - uses: actions/checkout@v4
      - run: echo "Running costly CI..."