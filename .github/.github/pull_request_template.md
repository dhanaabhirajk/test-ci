## Required CI Step

⚠️ Costly CI does NOT run automatically.

To run CI, comment on this PR:

```
/run-ci
```

✅ This is required before merge.

After commenting, wait for **Costly CI Check** to pass.